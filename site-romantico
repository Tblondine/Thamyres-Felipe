# Thamyres-Felipe
<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>O Acerto da Matrix 💖</title>
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Quicksand:wght@400;600&display=swap" rel="stylesheet"/>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background: linear-gradient(to bottom, #ffe0f0, #f3e5f5);
      font-family: 'Quicksand', sans-serif;
      color: #6a1b9a;
      text-align: center;
      overflow-x: hidden;
      transition: background 1s ease;
    }

    header {
      padding: 3rem 1rem 1rem;
      animation: fadeIn 1.5s ease-in-out;
    }

    h1 {
      font-family: 'Great Vibes', cursive;
      font-size: 3.5rem;
      color: #d81b60;
      margin-bottom: 0.2rem;
    }

    p {
      font-size: 1.2rem;
      margin: 0;
    }

    .video {
      margin: 2rem auto;
      max-width: 90%;
      animation: fadeInUp 2s ease;
    }

    video {
      width: 100%;
      border-radius: 20px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    }

    .galeria {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1.2rem;
      padding: 2rem 1rem;
      animation: fadeInUp 2.5s ease;
    }

    .galeria img {
      width: 220px;
      height: auto;
      border-radius: 20px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      opacity: 0;
      animation: aparecer 1s forwards;
    }

    .galeria img:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 24px rgba(0, 0, 0, 0.15);
    }

    footer {
      margin-top: 2rem;
      padding: 1.5rem;
      font-style: italic;
      font-size: 1.2rem;
      background-color: rgba(255, 255, 255, 0.4);
      border-top: 2px dashed #d81b60;
      animation: fadeIn 2.5s ease-in-out;
    }

    audio {
      display: none;
    }

    /* Animações */
    @keyframes fadeIn {
      from { opacity: 0 }
      to { opacity: 1 }
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px) }
      to { opacity: 1; transform: translateY(0) }
    }

    @keyframes aparecer {
      to { opacity: 1 }
    }
  </style>
</head>
<body>

  <!-- Música -->
  <audio autoplay loop>
    <source src="audio/next_by_me.mp3" type="audio/mp3">
    Seu navegador não suporta áudio.
  </audio>

  <!-- Cabeçalho -->
  <header>
    <h1>O Acerto da Matrix</h1>
    <p>Felipe & Você — desde 26/12/2024 ✨</p>
  </header>

  <!-- Vídeo -->
  <section class="video">
    <video src="video/reencontro.mp4" autoplay muted loop playsinline></video>
  </section>

  <!-- Galeria de Fotos -->
  <section class="galeria">
    <img src="imagens/foto1.jpg" style="animation-delay: 0.2s;">
    <img src="imagens/foto2.jpg" style="animation-delay: 0.4s;">
    <img src="imagens/foto3.jpg" style="animation-delay: 0.6s;">
    <img src="imagens/foto4.jpg" style="animation-delay: 0.8s;">
    <!-- Adicione mais se quiser -->
  </section>

  <!-- Frase especial -->
  <footer>
    “O acerto da matrix!” 💫 Com amor e conexão, sempre.
  </footer>

</body>
</html>
[mini-site-thamyres-felipe.zip](https://github.com/user-attachments/files/19615424/mini-site-thamyres-felipe.zip)
